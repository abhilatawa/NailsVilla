import { PagePlaceholder } from '@/components/layout/PagePlaceholder'

export function LoginPage() {
  return (
    <PagePlaceholder title="Log In" description="Account login is coming in a later phase." />
  )
}
